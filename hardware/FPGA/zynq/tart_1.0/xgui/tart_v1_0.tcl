# Definitional proc to organize widgets for parameters.
proc init_gui { IPINST } {
  ipgui::add_param $IPINST -name "Component_Name"

}

proc update_PARAM_VALUE.ACCUM_BITS { PARAM_VALUE.ACCUM_BITS } {
	# Procedure called to update ACCUM_BITS when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.ACCUM_BITS { PARAM_VALUE.ACCUM_BITS } {
	# Procedure called to validate ACCUM_BITS
	return true
}

proc update_PARAM_VALUE.ALIGN { PARAM_VALUE.ALIGN } {
	# Procedure called to update ALIGN when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.ALIGN { PARAM_VALUE.ALIGN } {
	# Procedure called to validate ALIGN
	return true
}

proc update_PARAM_VALUE.ANTENNAE { PARAM_VALUE.ANTENNAE } {
	# Procedure called to update ANTENNAE when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.ANTENNAE { PARAM_VALUE.ANTENNAE } {
	# Procedure called to validate ANTENNAE
	return true
}

proc update_PARAM_VALUE.ASYNC { PARAM_VALUE.ASYNC } {
	# Procedure called to update ASYNC when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.ASYNC { PARAM_VALUE.ASYNC } {
	# Procedure called to validate ASYNC
	return true
}

proc update_PARAM_VALUE.BANK_BITS { PARAM_VALUE.BANK_BITS } {
	# Procedure called to update BANK_BITS when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.BANK_BITS { PARAM_VALUE.BANK_BITS } {
	# Procedure called to validate BANK_BITS
	return true
}

proc update_PARAM_VALUE.BBITS { PARAM_VALUE.BBITS } {
	# Procedure called to update BBITS when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.BBITS { PARAM_VALUE.BBITS } {
	# Procedure called to validate BBITS
	return true
}

proc update_PARAM_VALUE.BLOCK { PARAM_VALUE.BLOCK } {
	# Procedure called to update BLOCK when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.BLOCK { PARAM_VALUE.BLOCK } {
	# Procedure called to validate BLOCK
	return true
}

proc update_PARAM_VALUE.BSB { PARAM_VALUE.BSB } {
	# Procedure called to update BSB when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.BSB { PARAM_VALUE.BSB } {
	# Procedure called to validate BSB
	return true
}

proc update_PARAM_VALUE.CDATA { PARAM_VALUE.CDATA } {
	# Procedure called to update CDATA when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.CDATA { PARAM_VALUE.CDATA } {
	# Procedure called to validate CDATA
	return true
}

proc update_PARAM_VALUE.CHECK { PARAM_VALUE.CHECK } {
	# Procedure called to update CHECK when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.CHECK { PARAM_VALUE.CHECK } {
	# Procedure called to validate CHECK
	return true
}

proc update_PARAM_VALUE.CONST { PARAM_VALUE.CONST } {
	# Procedure called to update CONST when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.CONST { PARAM_VALUE.CONST } {
	# Procedure called to validate CONST
	return true
}

proc update_PARAM_VALUE.C_M00_AXIS_TDATA_WIDTH { PARAM_VALUE.C_M00_AXIS_TDATA_WIDTH } {
	# Procedure called to update C_M00_AXIS_TDATA_WIDTH when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.C_M00_AXIS_TDATA_WIDTH { PARAM_VALUE.C_M00_AXIS_TDATA_WIDTH } {
	# Procedure called to validate C_M00_AXIS_TDATA_WIDTH
	return true
}

proc update_PARAM_VALUE.DEBUG { PARAM_VALUE.DEBUG } {
	# Procedure called to update DEBUG when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.DEBUG { PARAM_VALUE.DEBUG } {
	# Procedure called to validate DEBUG
	return true
}

proc update_PARAM_VALUE.DELAY { PARAM_VALUE.DELAY } {
	# Procedure called to update DELAY when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.DELAY { PARAM_VALUE.DELAY } {
	# Procedure called to validate DELAY
	return true
}

proc update_PARAM_VALUE.MSB { PARAM_VALUE.MSB } {
	# Procedure called to update MSB when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.MSB { PARAM_VALUE.MSB } {
	# Procedure called to validate MSB
	return true
}

proc update_PARAM_VALUE.MULTI { PARAM_VALUE.MULTI } {
	# Procedure called to update MULTI when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.MULTI { PARAM_VALUE.MULTI } {
	# Procedure called to validate MULTI
	return true
}

proc update_PARAM_VALUE.NSB { PARAM_VALUE.NSB } {
	# Procedure called to update NSB when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.NSB { PARAM_VALUE.NSB } {
	# Procedure called to validate NSB
	return true
}

proc update_PARAM_VALUE.PIPED { PARAM_VALUE.PIPED } {
	# Procedure called to update PIPED when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.PIPED { PARAM_VALUE.PIPED } {
	# Procedure called to validate PIPED
	return true
}

proc update_PARAM_VALUE.READ_BANKS { PARAM_VALUE.READ_BANKS } {
	# Procedure called to update READ_BANKS when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.READ_BANKS { PARAM_VALUE.READ_BANKS } {
	# Procedure called to validate READ_BANKS
	return true
}

proc update_PARAM_VALUE.READ_BITS { PARAM_VALUE.READ_BITS } {
	# Procedure called to update READ_BITS when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.READ_BITS { PARAM_VALUE.READ_BITS } {
	# Procedure called to validate READ_BITS
	return true
}

proc update_PARAM_VALUE.READ_COUNT { PARAM_VALUE.READ_COUNT } {
	# Procedure called to update READ_COUNT when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.READ_COUNT { PARAM_VALUE.READ_COUNT } {
	# Procedure called to validate READ_COUNT
	return true
}

proc update_PARAM_VALUE.RESET { PARAM_VALUE.RESET } {
	# Procedure called to update RESET when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.RESET { PARAM_VALUE.RESET } {
	# Procedure called to validate RESET
	return true
}

proc update_PARAM_VALUE.RNG { PARAM_VALUE.RNG } {
	# Procedure called to update RNG when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.RNG { PARAM_VALUE.RNG } {
	# Procedure called to validate RNG
	return true
}

proc update_PARAM_VALUE.TMUX_BITS { PARAM_VALUE.TMUX_BITS } {
	# Procedure called to update TMUX_BITS when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.TMUX_BITS { PARAM_VALUE.TMUX_BITS } {
	# Procedure called to validate TMUX_BITS
	return true
}

proc update_PARAM_VALUE.TMUX_RATE { PARAM_VALUE.TMUX_RATE } {
	# Procedure called to update TMUX_RATE when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.TMUX_RATE { PARAM_VALUE.TMUX_RATE } {
	# Procedure called to validate TMUX_RATE
	return true
}

proc update_PARAM_VALUE.VISB_LOG2 { PARAM_VALUE.VISB_LOG2 } {
	# Procedure called to update VISB_LOG2 when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.VISB_LOG2 { PARAM_VALUE.VISB_LOG2 } {
	# Procedure called to validate VISB_LOG2
	return true
}

proc update_PARAM_VALUE.VIZWR { PARAM_VALUE.VIZWR } {
	# Procedure called to update VIZWR when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.VIZWR { PARAM_VALUE.VIZWR } {
	# Procedure called to validate VIZWR
	return true
}

proc update_PARAM_VALUE.WBADR_BITS { PARAM_VALUE.WBADR_BITS } {
	# Procedure called to update WBADR_BITS when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.WBADR_BITS { PARAM_VALUE.WBADR_BITS } {
	# Procedure called to validate WBADR_BITS
	return true
}

proc update_PARAM_VALUE.WBBUS_BITS { PARAM_VALUE.WBBUS_BITS } {
	# Procedure called to update WBBUS_BITS when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.WBBUS_BITS { PARAM_VALUE.WBBUS_BITS } {
	# Procedure called to validate WBBUS_BITS
	return true
}

proc update_PARAM_VALUE.WSB { PARAM_VALUE.WSB } {
	# Procedure called to update WSB when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.WSB { PARAM_VALUE.WSB } {
	# Procedure called to validate WSB
	return true
}

proc update_PARAM_VALUE.XBITS { PARAM_VALUE.XBITS } {
	# Procedure called to update XBITS when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.XBITS { PARAM_VALUE.XBITS } {
	# Procedure called to validate XBITS
	return true
}

proc update_PARAM_VALUE.XSB { PARAM_VALUE.XSB } {
	# Procedure called to update XSB when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.XSB { PARAM_VALUE.XSB } {
	# Procedure called to validate XSB
	return true
}

proc update_PARAM_VALUE.C_S00_AXI_DATA_WIDTH { PARAM_VALUE.C_S00_AXI_DATA_WIDTH } {
	# Procedure called to update C_S00_AXI_DATA_WIDTH when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.C_S00_AXI_DATA_WIDTH { PARAM_VALUE.C_S00_AXI_DATA_WIDTH } {
	# Procedure called to validate C_S00_AXI_DATA_WIDTH
	return true
}

proc update_PARAM_VALUE.C_S00_AXI_ADDR_WIDTH { PARAM_VALUE.C_S00_AXI_ADDR_WIDTH } {
	# Procedure called to update C_S00_AXI_ADDR_WIDTH when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.C_S00_AXI_ADDR_WIDTH { PARAM_VALUE.C_S00_AXI_ADDR_WIDTH } {
	# Procedure called to validate C_S00_AXI_ADDR_WIDTH
	return true
}

proc update_PARAM_VALUE.C_S00_AXI_BASEADDR { PARAM_VALUE.C_S00_AXI_BASEADDR } {
	# Procedure called to update C_S00_AXI_BASEADDR when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.C_S00_AXI_BASEADDR { PARAM_VALUE.C_S00_AXI_BASEADDR } {
	# Procedure called to validate C_S00_AXI_BASEADDR
	return true
}

proc update_PARAM_VALUE.C_S00_AXI_HIGHADDR { PARAM_VALUE.C_S00_AXI_HIGHADDR } {
	# Procedure called to update C_S00_AXI_HIGHADDR when any of the dependent parameters in the arguments change
}

proc validate_PARAM_VALUE.C_S00_AXI_HIGHADDR { PARAM_VALUE.C_S00_AXI_HIGHADDR } {
	# Procedure called to validate C_S00_AXI_HIGHADDR
	return true
}


proc update_MODELPARAM_VALUE.C_S00_AXI_DATA_WIDTH { MODELPARAM_VALUE.C_S00_AXI_DATA_WIDTH PARAM_VALUE.C_S00_AXI_DATA_WIDTH } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.C_S00_AXI_DATA_WIDTH}] ${MODELPARAM_VALUE.C_S00_AXI_DATA_WIDTH}
}

proc update_MODELPARAM_VALUE.C_S00_AXI_ADDR_WIDTH { MODELPARAM_VALUE.C_S00_AXI_ADDR_WIDTH PARAM_VALUE.C_S00_AXI_ADDR_WIDTH } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.C_S00_AXI_ADDR_WIDTH}] ${MODELPARAM_VALUE.C_S00_AXI_ADDR_WIDTH}
}

proc update_MODELPARAM_VALUE.ANTENNAE { MODELPARAM_VALUE.ANTENNAE PARAM_VALUE.ANTENNAE } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.ANTENNAE}] ${MODELPARAM_VALUE.ANTENNAE}
}

proc update_MODELPARAM_VALUE.NSB { MODELPARAM_VALUE.NSB PARAM_VALUE.NSB } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.NSB}] ${MODELPARAM_VALUE.NSB}
}

proc update_MODELPARAM_VALUE.ALIGN { MODELPARAM_VALUE.ALIGN PARAM_VALUE.ALIGN } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.ALIGN}] ${MODELPARAM_VALUE.ALIGN}
}

proc update_MODELPARAM_VALUE.DEBUG { MODELPARAM_VALUE.DEBUG PARAM_VALUE.DEBUG } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.DEBUG}] ${MODELPARAM_VALUE.DEBUG}
}

proc update_MODELPARAM_VALUE.MULTI { MODELPARAM_VALUE.MULTI PARAM_VALUE.MULTI } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.MULTI}] ${MODELPARAM_VALUE.MULTI}
}

proc update_MODELPARAM_VALUE.RNG { MODELPARAM_VALUE.RNG PARAM_VALUE.RNG } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.RNG}] ${MODELPARAM_VALUE.RNG}
}

proc update_MODELPARAM_VALUE.CONST { MODELPARAM_VALUE.CONST PARAM_VALUE.CONST } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.CONST}] ${MODELPARAM_VALUE.CONST}
}

proc update_MODELPARAM_VALUE.CDATA { MODELPARAM_VALUE.CDATA PARAM_VALUE.CDATA } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.CDATA}] ${MODELPARAM_VALUE.CDATA}
}

proc update_MODELPARAM_VALUE.ASYNC { MODELPARAM_VALUE.ASYNC PARAM_VALUE.ASYNC } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.ASYNC}] ${MODELPARAM_VALUE.ASYNC}
}

proc update_MODELPARAM_VALUE.PIPED { MODELPARAM_VALUE.PIPED PARAM_VALUE.PIPED } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.PIPED}] ${MODELPARAM_VALUE.PIPED}
}

proc update_MODELPARAM_VALUE.RESET { MODELPARAM_VALUE.RESET PARAM_VALUE.RESET } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.RESET}] ${MODELPARAM_VALUE.RESET}
}

proc update_MODELPARAM_VALUE.CHECK { MODELPARAM_VALUE.CHECK PARAM_VALUE.CHECK } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.CHECK}] ${MODELPARAM_VALUE.CHECK}
}

proc update_MODELPARAM_VALUE.VIZWR { MODELPARAM_VALUE.VIZWR PARAM_VALUE.VIZWR } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.VIZWR}] ${MODELPARAM_VALUE.VIZWR}
}

proc update_MODELPARAM_VALUE.DELAY { MODELPARAM_VALUE.DELAY PARAM_VALUE.DELAY } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.DELAY}] ${MODELPARAM_VALUE.DELAY}
}

proc update_MODELPARAM_VALUE.WSB { MODELPARAM_VALUE.WSB PARAM_VALUE.WSB } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.WSB}] ${MODELPARAM_VALUE.WSB}
}

proc update_MODELPARAM_VALUE.BSB { MODELPARAM_VALUE.BSB PARAM_VALUE.BSB } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.BSB}] ${MODELPARAM_VALUE.BSB}
}

proc update_MODELPARAM_VALUE.MSB { MODELPARAM_VALUE.MSB PARAM_VALUE.MSB } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.MSB}] ${MODELPARAM_VALUE.MSB}
}

proc update_MODELPARAM_VALUE.XSB { MODELPARAM_VALUE.XSB PARAM_VALUE.XSB } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.XSB}] ${MODELPARAM_VALUE.XSB}
}

proc update_MODELPARAM_VALUE.ACCUM_BITS { MODELPARAM_VALUE.ACCUM_BITS PARAM_VALUE.ACCUM_BITS } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.ACCUM_BITS}] ${MODELPARAM_VALUE.ACCUM_BITS}
}

proc update_MODELPARAM_VALUE.VISB_LOG2 { MODELPARAM_VALUE.VISB_LOG2 PARAM_VALUE.VISB_LOG2 } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.VISB_LOG2}] ${MODELPARAM_VALUE.VISB_LOG2}
}

proc update_MODELPARAM_VALUE.READ_BANKS { MODELPARAM_VALUE.READ_BANKS PARAM_VALUE.READ_BANKS } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.READ_BANKS}] ${MODELPARAM_VALUE.READ_BANKS}
}

proc update_MODELPARAM_VALUE.BANK_BITS { MODELPARAM_VALUE.BANK_BITS PARAM_VALUE.BANK_BITS } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.BANK_BITS}] ${MODELPARAM_VALUE.BANK_BITS}
}

proc update_MODELPARAM_VALUE.WBADR_BITS { MODELPARAM_VALUE.WBADR_BITS PARAM_VALUE.WBADR_BITS } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.WBADR_BITS}] ${MODELPARAM_VALUE.WBADR_BITS}
}

proc update_MODELPARAM_VALUE.WBBUS_BITS { MODELPARAM_VALUE.WBBUS_BITS PARAM_VALUE.WBBUS_BITS } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.WBBUS_BITS}] ${MODELPARAM_VALUE.WBBUS_BITS}
}

proc update_MODELPARAM_VALUE.TMUX_RATE { MODELPARAM_VALUE.TMUX_RATE PARAM_VALUE.TMUX_RATE } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.TMUX_RATE}] ${MODELPARAM_VALUE.TMUX_RATE}
}

proc update_MODELPARAM_VALUE.TMUX_BITS { MODELPARAM_VALUE.TMUX_BITS PARAM_VALUE.TMUX_BITS } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.TMUX_BITS}] ${MODELPARAM_VALUE.TMUX_BITS}
}

proc update_MODELPARAM_VALUE.READ_COUNT { MODELPARAM_VALUE.READ_COUNT PARAM_VALUE.READ_COUNT } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.READ_COUNT}] ${MODELPARAM_VALUE.READ_COUNT}
}

proc update_MODELPARAM_VALUE.READ_BITS { MODELPARAM_VALUE.READ_BITS PARAM_VALUE.READ_BITS } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.READ_BITS}] ${MODELPARAM_VALUE.READ_BITS}
}

proc update_MODELPARAM_VALUE.C_M00_AXIS_TDATA_WIDTH { MODELPARAM_VALUE.C_M00_AXIS_TDATA_WIDTH PARAM_VALUE.C_M00_AXIS_TDATA_WIDTH } {
	# Procedure called to set VHDL generic/Verilog parameter value(s) based on TCL parameter value
	set_property value [get_property value ${PARAM_VALUE.C_M00_AXIS_TDATA_WIDTH}] ${MODELPARAM_VALUE.C_M00_AXIS_TDATA_WIDTH}
}

